The example output (`csaf_example_output.json`) was generated using the following command:
```bash
go run cmd/main.go "https://github.com/golang-jwt/jwt/security/advisories/GHSA-mh63-6h87-95cp" "examples/csaf_example_output.json"
```

For comparison purposes, the GHSA with the ID `GHSA-mh63-6h87-95cp` is also placed as `GHSA-mh63-6h87-95cp.json` in the `examples` directory.`